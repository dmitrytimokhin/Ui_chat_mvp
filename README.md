# 🧠 Локальный LLM-чат с поддержкой Ollama и Qwen

Этот проект представляет собой **локальный чат с языковыми моделями**, работающий полностью автономно (без подключения к облачным API). Он объединяет два мощных подхода:
- **Ollama** — для быстрого запуска лёгких моделей (`phi3`) через HTTP API.
- **Transformers (Hugging Face)** — для запуска локальной модели **Qwen3-1.7B** прямо в памяти.

Архитектура:
```
Streamlit (UI) → FastAPI (бэкенд) → LLM (Ollama или Qwen)
```
---

## 🛠️ Требования

- Python 3.10+
- `ollama` (установлен и запущен локально)
- (Опционально) GPU или мощный CPU (для Qwen)
- Рекомендуется: macOS (M1/M2) — проект оптимизирован под ARM

---

## 📦 Установка зависимостей

1. Создайте и активируйте виртуальное окружение:

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Установите модель `phi3` в Ollama:

```bash
ollama pull phi3
```

> ⚠️ Убедитесь, что `ollama serve` запущен (обычно запускается автоматически при первом вызове `ollama`).

---

## ▶️ Запуск проекта

### 1. Запустите FastAPI-бэкенд

В отдельном терминале:

```bash
uvicorn fastapi_llm.main:app --host 0.0.0.0 --port 8000 --reload
```

> FastAPI будет доступен по адресу: http://localhost:8000

### 2. Запустите Streamlit-фронтенд

В другом терминале (в той же среде):

```bash
streamlit run streamlit.py --server.port=8501
```

> Интерфейс откроется автоматически в браузере: http://localhost:8501

---

## 🧪 Режимы работы

В боковой панели Streamlit выберите:

- **`phi3_ollama`** — быстрая модель, минимальное потребление RAM, работает через `ollama`.
- **`qwen_transformers`** — более точная модель (1.7B параметров), но требует:
  - или MPS/GPU для ускорения (на M1/M2 активируется автоматически)

> При первом запуске Qwen загрузится из Hugging Face.

---

## 📏 Управление контекстом

Система автоматически:
- **Подсчитывает токены** в истории и промпте.
- **Обрезает старые сообщения**, если суммарный контекст превышает ~3584 токена.
- Гарантирует, что модель не получит переполненный контекст.

---

## 🔐 Безопасность

- Все данные остаются **локально**.
- Нет подключения к внешним API.
- FastAPI разрешает CORS **только для `localhost:8501`** (Streamlit).

---

## 📝 Примечания для M1/M2

- Ollama отлично оптимизирована для Apple Silicon.
- Qwen будет использовать **MPS (Metal Performance Shaders)** автоматически, если `torch` установлен с поддержкой MPS.
- Для лучшей производительности Qwen можно использовать `--device mps` (уже включено через `device_map="auto"`).

---

## 📁 Структура проекта

```
./
├── streamlit.py              # UI-интерфейс
├── fastapi_llm/
│   ├── main.py               # FastAPI-эндпоинт /chat
│   ├── models.py             # Pydantic-модели
│   ├── llm_ollama.py         # Интеграция с Ollama
│   ├── llm_qwen.py           # Интеграция с Qwen через transformers
│   └── token_utils.py        # Обрезка истории по токенам
└── README.md
```

---

## 🙌 Автор

Dmitry  
GitHub: [@dmitrytimokhin](https://github.com/dmitrytimokhin)
```