# Локальный вопрос-ответный чат (MVP)

Простой, полностью автономный чат на базе:
- **Streamlit** — для UI,
- **Ollama** — для запуска локальной LLM,
- **Модель `phi3`** — маленькая, быстрая и отлично работающая на Mac M1.

Нет облака, нет API-ключей, нет зависимости от интернета — только ваш компьютер.

---

## 🚀 Возможности

- Интерактивный чат с историей.
- Системная инструкция задана в коде (настраивается в `app.py`).
- Автоматическое ограничение контекста до **32 000 слов** (с удалением старых сообщений).
- Отображение последних **10 сообщений** в чате.
- Страница **«Полный диалог»** — просмотр всей истории.
- Экспорт диалога в **`.txt`** файл.

---

## 🛠 Требования

- Python 3.9+
- [Ollama](https://ollama.com/) (установлен и запущен)
- Модель `phi3` (или другая — см. ниже)

---

## ▶️ Запуск

1. Установите зависимости:
   ```bash
   pip install -r rquirements.txt
2. Напишите в terminal `ollama run phi3` или `ollama serve`
3. Запустите streamlit через `streamlit run app.py`