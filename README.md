# 🧠 Локальный LLM-чат с поддержкой Ollama и Qwen
# Ui_chat_mvp

Лёгкий локальный чат-приложение: Streamlit UI + FastAPI backend, которое маршрутизует запросы к локальным LLM-движкам (Ollama, vLLM/Qwen).

Ключевые возможности
- Лёгкий Streamlit-интерфейс для диалогов
- Backend на FastAPI с адаптерами для Ollama и Qwen/vLLM
- Модели подгружаются при старте сервера (не при каждом запросе)
- Очистка памяти (GC, CUDA/MPS) между вызовами для стабильности
- Поддержка нескольких диалогов в UI с сохранением на диск

Структура репозитория (важные файлы)
- **Frontend:** [chat_ui.py](chat_ui.py)
- **Backend (API):** [fastapi_llm/api/main.py](fastapi_llm/api/main.py)
- **Адаптеры моделей:** [fastapi_llm/models/ollama/llm_ollama.py](fastapi_llm/models/ollama/llm_ollama.py), [fastapi_llm/models/transformers/llm_qwen.py](fastapi_llm/models/transformers/llm_qwen.py)
- **Утилиты и схемы:** [fastapi_llm/utils/utils.py](fastapi_llm/utils/utils.py), [fastapi_llm/utils/entities.py](fastapi_llm/utils/entities.py)
- **Docker:** [Dockerfile.app](Dockerfile.app), [docker-compose.yaml](docker-compose.yaml)

Быстрый запуск для разработки

1) Установите зависимости (рекомендуется виртуальное окружение):

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2) Запустите backend:

```bash
uvicorn fastapi_llm.main:app --host 0.0.0.0 --port 8000 --reload
```

3) Запустите UI в другом терминале:

```bash
streamlit run chat_ui.py --server.port=8501 --server.address=0.0.0.0
```

Docker (контейнерный режим)

Собрать и запустить (docker-compose):

```bash
docker-compose build
docker-compose up --force-recreate
```

Dockerfile.app по умолчанию запускает одновременно `uvicorn fastapi_llm.api.main:app` и `streamlit run chat_ui.py`.

Конфигурация и переменные среды
- OLLAMA_URL — URL Ollama сервера (по умолчанию host.docker.internal в контейнере)
- VLLM_URL — URL vLLM/OpenAI-совместимого сервера (если используется)
- Другие настройки задаются прямо в UI (выбор модели, температура, max_tokens и т.д.)

Архитектура и рабочий поток
- UI отправляет POST /chat на backend с телом: prompt, history, model_alias, temperature, max_tokens
- Backend диспатчит по `model_alias` в адаптеры: `ollama` → llm_ollama, `qwen_vllm` → llm_qwen
- Адаптеры используют `truncate_history()` из utils, чтобы уместить контекст в лимит токенов
- Модели загружаются в `init_models()` при старте FastAPI (lifespan startup)

Полезные советы и отладка
- Если сервер перезапускается при Ctrl+C из-за dev-сервера с `--reload`, остановите процесс ещё раз или используйте `pkill -f uvicorn`.
- Для проблем с памятью проверьте логи — проект вызывает `gc.collect()` и `torch.cuda.empty_cache()`/`torch.mps.empty_cache()` где это применимо.
- Если вы меняли структуру модулей, используйте `uvicorn fastapi_llm.api.main:app` или `fastapi_llm.main:app` (прокси для совместимости).

Где смотреть код
- Точка входа API: [fastapi_llm/api/main.py](fastapi_llm/api/main.py)
- Адаптер для Qwen/vLLM: [fastapi_llm/models/transformers/llm_qwen.py](fastapi_llm/models/transformers/llm_qwen.py)
- Адаптер для Ollama: [fastapi_llm/models/ollama/llm_ollama.py](fastapi_llm/models/ollama/llm_ollama.py)
- UI логика: [chat_ui.py](chat_ui.py)

Вклад и тестирование
- Откат/фиксаторы: после правок запустите backend + UI локально и прогоните несколько диалогов, проверив, что модели загружаются один раз при старте.
- По желанию можно добавить тесты для `truncate_history()` и для загрузки моделей.

Контакты
- Если нужна помощь с деплоем или тестированием контейнера — напишите issue или запустите меня локально в этом репозитории.

---
Коротко: проект — локальный LLM-чат, модели подгружаются при старте, UI поддерживает множественные диалоги и сохраняет истории на диск.

```

2) (Если используете Ollama) загрузите модель `phi3` и запустите Ollama:

```bash
ollama pull phi3
ollama serve
```

3) Запустите бэкенд и фронтенд (в двух терминалах):

```bash
# backend
uvicorn fastapi_llm.main:app --host 0.0.0.0 --port 8000 --reload

# frontend
streamlit run chat_ui.py --server.port=8501
```

API: http://localhost:8000
UI:  http://localhost:8501

---

**Docker (dev)**

Проект содержит `Dockerfile.app` и `docker-compose.yaml` для быстрого запуска. Текущий Dockerfile запускает backend и Streamlit в одном контейнере (удобно для разработки, не оптимально для production).

Запуск через Docker Compose:

```bash
docker-compose up --build
```

Рекомендация для production: разделить сервисы (отдельные контейнеры для API и UI), использовать процессный менеджер (gunicorn/uvicorn workers) и внешние volume/env-файлы.

---

**Модельные алиасы**
- `phi3_ollama` — запросы перенаправляются в Ollama (env: `OLLAMA_URL` по умолчанию).
- `qwen_transformers` — локальная загрузка через `transformers`. При работе с Qwen в системную подсказку добавляется `/no_think`.

---

**Управление контекстом**
- Контекст (история + prompt) автоматически обрезается по токенам.
- Токенизация и логика усечений находятся в `fastapi_llm/utils.py`.

---

**Что улучшено в рефакторинге**
- `fastapi_llm/utils.py`: единое место для подсчёта токенов, обрезки истории, системного промпта и логирования.
- Единый тип ошибок `EngineError` для адаптеров — позволяет `main.py` корректно разделять ошибки движка и ошибки приложения.
- `llm_qwen.py`: теперь передаёт `input_ids` и `attention_mask` в `model.generate(...)` и использует `GenerationConfig` — предупреждения transformers устранены.

---

**Рекомендации / next steps**
- Добавить unit-тесты для `utils.truncate_history`.
- Добавить health/readiness endpoint и метрики (Prometheus) в `fastapi_llm/main.py`.
- Настроить CI (lint, tests, build image).
- Пересмотреть Dockerfile: разделить UI/API, использовать production-ready process manager.

---

**Структура (основное)**

- `chat_ui.py` — Streamlit UI.
- `fastapi_llm/main.py` — FastAPI приложение.
- `fastapi_llm/models.py` — Pydantic-модели запроса/ответа.
- `fastapi_llm/llm_ollama.py` — адаптер Ollama.
- `fastapi_llm/llm_qwen.py` — адаптер Qwen (transformers).
- `fastapi_llm/utils.py` — общие утилиты (токенизация / truncate / логирование).

---

Автор: Dmitry — улучшения: интеграция утилит, унификация ошибок и исправления генерации для Qwen.
